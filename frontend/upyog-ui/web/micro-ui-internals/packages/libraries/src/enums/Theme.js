const ThemeTypes = Object.freeze({
  CITIZEN: "CITIZEN",
  EMPLOYEE: "EMPLOYEE",
});

const ThemeStatus = Object.freeze({
  DEFAULT: "DEFAULT",
});

export { ThemeTypes, ThemeStatus };
export default ThemeTypes;
