import Pages from "./Pages";
import ConfigActionTypes from "./ConfigActionTypes";
import Theme, { ThemeTypes, ThemeStatus } from "./Theme";
// import FormFields from "./FormFields;"

const Enums = { Pages, ConfigActionTypes, Theme, ThemeTypes, ThemeStatus };

export default Enums;
export { Theme, ThemeTypes, ThemeStatus };

